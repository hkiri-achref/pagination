
spring:
  datasource:
    url: jdbc:mysql://localhost:6033/pagination
    #?createDatabaseIfNoExist=true&useUnicode=true&useJDBCCompliantTimezoneShift=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
  ## Specify DBMS
  jpa:
    database: MYSQL
    show-sql: true


    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

server:
  port: 8080
